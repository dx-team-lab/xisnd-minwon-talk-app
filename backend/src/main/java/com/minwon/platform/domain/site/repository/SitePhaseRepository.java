package com.minwon.platform.domain.site.repository;

import com.minwon.platform.domain.site.entity.SitePhase;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SitePhaseRepository extends JpaRepository<SitePhase, Long> {
}
